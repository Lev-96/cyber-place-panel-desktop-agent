# Production env consumed by Vite at BUILD time (default `vite build`).
# The staging counterpart is `.env.staging` (vite build --mode staging).
# Not secret — Vite inlines this into the client bundle.
VITE_BACKEND_URL=https://cyber-place-server-production.up.railway.app
